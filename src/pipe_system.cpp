#include "pipe_system.h"


void PipeSystem::Process(std::vector<GameObject*>& list, double deltaTime)
{
	//get the pipes rendering component
	//generate a mesh
	//align the positions and rotations
	//set the mesh to the rendering component
	//hope for the best
}
